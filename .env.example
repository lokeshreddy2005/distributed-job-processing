# Copy to .env and set your own value before running `docker compose up`
# (or before running the API/worker locally with a non-default Postgres
# password). .env is gitignored - never commit real credentials.
POSTGRES_PASSWORD=changeme-set-your-own-password
