# Copy to .env and adjust before running `docker compose up` (or before
# running the API/worker locally with a non-default Postgres password).
# .env is gitignored - never commit real credentials.
POSTGRES_PASSWORD=postgres
